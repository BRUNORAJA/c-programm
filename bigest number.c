#include<stdio.h>
#include<conio.h>
void main()
{
int a,b,c;
clrscr();
scanf("%d%d",&a&b);
if(a>b)
{
printf("\n the A value is higher than the B value%d",a);
}
else
{
printf("\n the value is higher than the a%d",b);
}
getch();
}
